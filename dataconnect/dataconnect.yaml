specVersion: "v1"
serviceId: "kopikenangan-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kopikenangan-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
